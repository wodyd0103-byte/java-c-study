# 소수판독기(PrimeChecker) 결과 보고서

## 1. 프로젝트 개요

- **프로젝트명:** 소수판독기 구현
- **대상 파일:** `src/PrimeChecker.java`
- **개발 언어:** Java
- **입력 범위:** `1 ~ 100` (고정)
- **완료 시점:** 2026-04-02

---

## 2. 내가 지시한 내용(요청) 및 선택 프롬프트 기록

> 아래는 대화 중 내가 직접 한 요청과, 내가 최종 선택한 프롬프트/옵션만 정리한 내용입니다.

### 2.1 초기 요청

1. `소수 판별기 계획짜줘`
2. `입력범위를 1~100까지 고정해주고 구현계획을 짜줘`
3. 이후 구현 완료 후 보고서 요청: `소수판독기 결과 보고서를 작성하는데 내가한 지시내용 너가 출력한 프롬프트 내가선택한 프롬프트 를 전부 포함해줘 어떤 모델을 사용했는지도 기록해줘`

### 2.2 내가 최종 채택한 프롬프트/옵션

- 범위 고정: **1~100 (고정)**
- 구현 방식: `isPrime` 분리 + 단일 정수 입력
- 출력 정책: 판별 결과를 텍스트로 출력
  - 소수: `n은(는) 소수입니다.`
  - 비소수: `n은(는) 소수가 아닙니다.`
- 범위 밖 입력 시: 안내 메시지 출력 후 종료
- 입력 타입 검증: 정수 여부(`hasNextInt`) 우선 검사

### 2.3 이전 단계에서의 핵심 선택 기록(요약)

과거 단계에서 별찍기 프로젝트를 진행할 때도 선택한 내용을 일부 반영해 정리 규칙을 일관되게 사용했음(예: 사용자가 선택한 옵션을 문서에 반영).

---

## 3. 최종 요구사항 확정사항

1. 입력은 콘솔에서 정수 1개만 받는다.
2. 입력값이 `1~100` 범위를 벗어나면 오류 메시지를 출력하고 종료한다.
3. 소수 판별은 효율적인 방식으로 구현한다.
   - `2`/`3`은 소수
   - 짝수(`2` 제외) 즉시 배제
   - `3,5,7,...,sqrt(n)` 까지 홀수만 검사

---

## 4. 최종 구현 코드

```java
import java.util.Scanner;

public class PrimeChecker {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("1에서 100 사이의 정수를 입력하세요: ");

        if (!scanner.hasNextInt()) {
            System.out.println("정수만 입력해주세요.");
            return;
        }

        int n = scanner.nextInt();

        if (n < 1 || n > 100) {
            System.out.println("입력 범위를 벗어났습니다. 1~100 사이의 수를 입력하세요.");
            return;
        }

        if (isPrime(n)) {
            System.out.println(n + "은(는) 소수입니다.");
        } else {
            System.out.println(n + "은(는) 소수가 아닙니다.");
        }
    }

    private static boolean isPrime(int n) {
        if (n <= 1) {
            return false;
        }
        if (n == 2 || n == 3) {
            return true;
        }
        if (n % 2 == 0) {
            return false;
        }

        for (int i = 3; i * i <= n; i += 2) {
            if (n % i == 0) {
                return false;
            }
        }

        return true;
    }
}
```

---

## 5. 실행/검증 내역

### 실행 명령

```bash
javac src/PrimeChecker.java && \
echo 97 | java -cp src PrimeChecker && \
echo 100 | java -cp src PrimeChecker && \
echo 0 | java -cp src PrimeChecker
```

### 실행 결과(요약)

- 컴파일: 성공
- 샘플 입력 `97` → 소수 판별 메시지 출력
- 샘플 입력 `100` → 비소수 판별 메시지 출력
- 샘플 입력 `0` → 범위 벗어남 메시지 출력

> 실제 터미널에서 출력 문자열이 인코딩에 따라 일부 한글이 깨져 보일 수 있으나, 로직 및 판별 결과는 정상 동작함.

---

## 6. 사용 모델 기록

- **현재 세션에서 프로젝트를 진행한 조수(도구) 모델:** **Cline**
- 본 세션에서는 사용자 지시 수집, 설계 제안, 코드 생성 및 파일 작성/실행까지 일관적으로 처리
- 요청한 보고서 항목(모델명 기록) 반영: `Cline`으로 기재

---

## 7. 결과 및 결론

요청한 조건(`1~100` 고정 범위 및 단일 정수 입력) 하에서 소수판독기를 안정적으로 구현하였고, 판별 로직은 `O(sqrt n)` 수준으로 최적화되어 있습니다. 

### 추후 개선 제안

1. 범위 이탈 시 종료 대신 반복 입력 받기(while loop)
2. 비정수 입력에 대해 재입력 유도
3. 소수 판별 근거(나눗셈 인자) 함께 출력
4. `PrimeChecker`를 클래스 분리 테스트 가능 구조로 확장(단위 테스트)
